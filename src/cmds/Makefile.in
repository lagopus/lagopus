TOPDIR		= @TOPDIR@
MKRULESDIR	= @MKRULESDIR@

RTE_SDK		= @RTE_SDK@
RTE_TARGET	= @RTE_TARGET@
RTE_LIBS	= @RTE_LIBS@

TARGET_EXE		= lagopus
INSTALL_SBIN_EXE_TARGETS	= $(TARGET_EXE)
INSTALL_SBIN_EXE_DIR		= $(DEST_SBIN_EXEDIR)

TARGETS			= $(TARGET_EXE)

SRCS		= lagopus.c modulesub.c

CPPFLAGS	+= -I$(BUILD_SRCDIR)/agent
CPPFLAGS	+= -I$(BUILD_CONFIGDIR)

DEP_LIBS	+= $(DEP_LAGOPUS_DATAPLANE_LIB)
DEP_LIBS	+= $(DEP_LAGOPUS_AGENT_LIB)
DEP_LIBS	+= $(DEP_LAGOPUS_CONFIG_LIB)
DEP_LIBS	+= $(DEP_LAGOPUS_UTIL_LIB)

LDFLAGS		+= $(RTE_LIBS) 

LDFLAGS		+= @PCAP_LIBS@

include $(MKRULESDIR)/vars.mk
include $(MKRULESDIR)/rules.mk
include .depend
